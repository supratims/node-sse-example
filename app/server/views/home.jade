- user = typeof(user) != 'undefined' ? user : { }
extends layout

title Home

block content
	div.container(style="margin-top:40px")
		h3 Welcome, #{user.username}
		div.col-xs-12.main
block scripts
	include scripts
	script(src='/home.js')